[pytest]
testpaths = tests
addopts = -ra -q --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    slow: tests that take more than ~5s (skip with -m 'not slow')
